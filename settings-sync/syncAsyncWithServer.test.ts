describe("syncAsyncWithServer tests", () => {
  describe("settingsSync tests", () => {
    test("recognises difference in dates: async greater than server = replace data", () => {})
    test("should take object from async and compare with server object", () => {})
    test("settingsSync compares data and ")
  })
})
