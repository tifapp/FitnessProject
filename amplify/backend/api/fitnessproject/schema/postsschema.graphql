type Post @model
@key(fields: ["timestamp", "userId"])
@key(name: "ByGroup", fields: ["group", "timestamp"], queryField: "postsByGroup")
{
  timestamp: AWSTimestamp!
  userId: String!
  description: String
  group: String
}