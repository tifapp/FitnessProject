#set($mapdata = [])
#foreach($item in ${ctx.args.ids})
    $util.qr($mapdata.add($util.dynamodb.toMapValues($item)))
#end

{
    "version" : "2018-05-29",
    "operation" : "BatchGetItem",
    "tables" : {
        "Conversation-4lwfnmtnpvb55mcushmhexhjgu-dev": {
			"keys": $utils.toJson($mapdata)
		}
    }
}