type Verification @model @auth(rules: [{allow: owner, ownerField: "id"}, {allow: private, provider: iam, operations: [read, update, delete]}, {allow: groups, groups: ["Admins"], operations: [read, update, delete]}]) {
  id: ID!
  title: String
  isVerified: Boolean
}
